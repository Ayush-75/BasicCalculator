<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Calculator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/status_bar</item>
    </style>


    <style name="CalculatorButton">
<item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_margin">2dp</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">24sp</item>
        <item name="background">@drawable/circle</item>
        <item name="backgroundTint">@color/circle_button_color</item>
        <item name="android:clickable">true</item>
        <item name="android:foreground">?android:attr/selectableItemBackground</item>
    </style>
    
    <style name="AllClearButton" parent="CalculatorButton">
        <item name="backgroundTint">@color/all_clear_button</item>
    </style>
    
    <style name="OperatorButton" parent="CalculatorButton">
        <item name="backgroundTint">@color/operatorButton</item>
    </style>

    <style name="EqualButton" parent="CalculatorButton">
        <item name="backgroundTint">@color/equal_button_background</item>
    </style>

    <style name="Theme.Calculator" parent="Base.Theme.Calculator" />
</resources>